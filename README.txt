All you have to do to run it is:

python send_friends_a_message.py <access token> <list of phone numbers comma separated> <string message>

You can get the access token from push bullet (You will need to setup a push bullet account and login with your device)